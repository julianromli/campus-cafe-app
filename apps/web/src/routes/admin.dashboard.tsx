import PagePlaceholder from "@/components/page-placeholder";

export default function AdminDashboardPage() {
  return (
    <PagePlaceholder
      title="Admin Dashboard"
      description="Route admin dashboard sudah aktif di bawah shell berotorisasi. Widget analytics akan ditambahkan setelah query analytics tersedia."
    />
  );
}
