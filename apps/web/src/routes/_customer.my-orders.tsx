import PagePlaceholder from "@/components/page-placeholder";

export default function MyOrdersPage() {
  return (
    <PagePlaceholder
      title="My Orders"
      description="Route customer untuk order tracking sudah tersedia. Status live dan order history akan menyusul saat modul orders dibangun."
    />
  );
}
