import PagePlaceholder from "@/components/page-placeholder";

export default function MyReservationsPage() {
  return (
    <PagePlaceholder
      title="My Reservations"
      description="Route dan guard customer untuk daftar reservasi sudah siap. Data reservation card akan masuk setelah mutation/query reservasi dibangun."
    />
  );
}
