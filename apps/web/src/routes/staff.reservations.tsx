import PagePlaceholder from "@/components/page-placeholder";

export default function StaffReservationsPage() {
  return (
    <PagePlaceholder
      title="Staff Reservations"
      description="Staff route untuk live reservation board sudah siap. Sprint tables akan mengisi floor plan operasional dan aksi release/occupied."
    />
  );
}
