import PagePlaceholder from "@/components/page-placeholder";

export default function AdminEventsPage() {
  return (
    <PagePlaceholder
      title="Admin Events"
      description="Route admin event management sudah siap. CRUD event dan publish flow akan menjadi fokus sprint events."
    />
  );
}
