import PagePlaceholder from "@/components/page-placeholder";

export default function AdminPaymentsPage() {
  return (
    <PagePlaceholder
      title="Admin Payments"
      description="Route admin payments sudah siap. Sync status dan payment operations akan ditambahkan setelah integrasi Mayar tersedia."
    />
  );
}
