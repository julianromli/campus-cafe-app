import PagePlaceholder from "@/components/page-placeholder";

export default function AdminEventAttendeesPage() {
  return (
    <PagePlaceholder
      title="Event Attendees"
      description="Route admin untuk attendee list sudah tersedia. Tabel peserta, CSV export, dan reminder actions akan diisi pada sprint events lanjutan."
    />
  );
}
