import PagePlaceholder from "@/components/page-placeholder";

export default function ReservePage() {
  return (
    <PagePlaceholder
      title="Reserve a Table"
      description="Route customer untuk reservasi sudah siap di bawah customer shell. Interactive floor plan akan diimplementasikan pada sprint fitur tables."
    />
  );
}
