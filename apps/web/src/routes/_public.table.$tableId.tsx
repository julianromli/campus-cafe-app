import PagePlaceholder from "@/components/page-placeholder";

export default function TableLandingPage() {
  return (
    <PagePlaceholder
      title="Table QR Landing"
      description="Route QR untuk pemesanan meja sudah tersedia. Sprint menu dan ordering akan mengisi konten per-table dan cart flow."
    />
  );
}
