import PagePlaceholder from "@/components/page-placeholder";

export default function AdminTablesPage() {
  return (
    <PagePlaceholder
      title="Admin Tables"
      description="Route admin untuk layout editor sudah tersedia. Visual floor editor dan CRUD meja akan diimplementasikan pada sprint tables."
    />
  );
}
